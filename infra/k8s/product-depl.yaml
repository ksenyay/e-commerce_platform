apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product
  template:
    metadata:
      labels:
        app: product
    spec:
      containers:
        - name: product
          image: ksenyay/product:latest
          imagePullPolicy: Never
          env:
            - name: MONGO_URL
              value: "mongodb://product-mongo-srv:27017/product"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_SECRET
            - name: R2_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: r2-access-key-id
                  key: R2_ACCESS_KEY_ID
            - name: R2_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: r2-secret-access-key
                  key: R2_SECRET_ACCESS_KEY
            - name: R2_BUCKET_NAME
              value: "soundio-storage"
            - name: R2_ACCOUNT_ID
              value: "590885aa0a9d29e5d71a7a61ec6969aa"
            - name: R2_PUBLIC_URL
              value: "https://pub-6253a91849cf4221ab5108b450c39f1a.r2.dev"
          ports:
            - containerPort: 4001
---
apiVersion: v1
kind: Service
metadata:
  name: product-srv
spec:
  selector:
    app: product
  ports:
    - protocol: TCP
      port: 4001
      targetPort: 4001
